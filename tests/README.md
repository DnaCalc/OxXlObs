# tests

Repo-level tests and retained regression hooks live here as the harness widens.

Initial policy:
1. crate-local unit tests live under each `src/oxxlobs-*/` crate,
2. retained fixtures live under `docs/test-corpus/`,
3. retained human-readable run summaries live under `docs/test-runs/`.
