# scripts

Repo-local wrappers and repeatable validation commands.

Initial baseline:
1. `meta-check.ps1`
   - runs local Rust validation checks.
2. `invoke-excel-observation.ps1`
   - runs the W006 stable Windows Excel COM capture path for a declared scenario and output root.
